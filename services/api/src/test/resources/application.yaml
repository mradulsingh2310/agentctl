spring:
  datasource:
    url: jdbc:h2:mem:agentctl-test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver

agentctl:
  temporal:
    enabled: false
    address: localhost:7233
    namespace: default
    task-queue: agentctl-runs
  worker:
    enabled: false
